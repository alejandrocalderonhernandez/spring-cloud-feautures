server:
  port: 7890

spring:
  application:
    name: oauth

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${random.value}

feign:
  hystrix:
    enabled: true
